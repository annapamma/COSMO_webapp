source("../COSMO/bin/tools.R")
# pro_file <- "${pro_file}"
# rna_file <- "${rna_file}"
# sample_file <- "${sample_file}"
# out_dir <- "data_use"
# dir.create(out_dir)
# format_input_data(pro_file, rna_file, sample_file, out_dir = out_dir)
